# Sandbox-only dev flag. Mounts the Virtual Puck Hub at /dev/hub.
# Read by Vite via `import.meta.env.VITE_DEV_TOOLS` (see App.tsx).
# Prod builds either don't set this var (Vite's `production` mode) or
# explicitly leave it unset; the Hub module is tree-shaken from the bundle.
VITE_DEV_TOOLS=1
